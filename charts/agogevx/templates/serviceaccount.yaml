apiVersion: v1
kind: ServiceAccount
metadata:
  name: agogevx
  labels:
    {{- include "agogevx.commonLabels" . | nindent 4 }}
# No pod in v1 talks to the Kubernetes API, so the token is never mounted.
automountServiceAccountToken: false
